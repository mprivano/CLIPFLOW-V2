/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
 */

// Placeholder only. The visible ClipFlow UI is rendered by index.html + app.js + styles.css.
// Do not put live product UI changes here unless the app is intentionally migrated to React.
export default function App() {
  return <div></div>;
}
